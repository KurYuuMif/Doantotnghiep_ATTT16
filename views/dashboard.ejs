<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bảng điều khiển - Đồ án ATTT16</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f0f0;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
    }

    .container {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      padding: 30px;
      margin: 40px auto;
      width: 90%;
      max-width: 800px;
    }

    h2 {
      font-size: 2.5rem;
      color: #4CAF50;
      margin-bottom: 10px;
      text-align: center;
    }

    nav {
      text-align: center;
      margin-bottom: 20px;
    }

    nav a {
      margin: 0 10px;
      color: #007BFF;
      text-decoration: none;
      font-weight: bold;
    }

    nav a:hover {
      color: #0056b3;
    }

    form {
      margin-top: 20px;
      text-align: center;
    }

    input[type="file"] {
      padding: 10px;
    }

    button {
      padding: 10px 20px;
      background-color: #4CAF50;
      border: none;
      color: white;
      border-radius: 5px;
      margin-left: 10px;
      cursor: pointer;
    }

    button:hover {
      background-color: #45a049;
    }

    ul {
      list-style: none;
      padding: 0;
      margin-top: 20px;
    }

    li {
      padding: 10px;
      border-bottom: 1px solid #ddd;
    }

    li:last-child {
      border-bottom: none;
    }

    .file-actions a,
    .file-actions form {
      display: inline-block;
      margin-left: 10px;
    }

    .file-actions form {
      display: inline;
    }

    .file-actions button {
      background-color: #f44336;
    }

    .file-actions button:hover {
      background-color: #d32f2f;
    }

  </style>
</head>
<body>
  <div class="container">
    <h2>Chào mừng bạn!</h2>

    <nav>
      <a href="/settings"><i class="fas fa-cog"></i> Cài đặt tài khoản</a> |
      <a href="/logout"><i class="fas fa-sign-out-alt"></i> Đăng xuất</a>
    </nav>

    <hr>

    <form method="POST" action="/upload" enctype="multipart/form-data">
      <input type="file" name="file" required />
      <button><i class="fas fa-upload"></i> Tải lên</button>
    </form>

    <h3>File đã tải lên</h3>
    <ul>
      <% if (files.length === 0) { %>
        <li>Chưa có file nào</li>
      <% } else { %>
        <% files.forEach(f => { %>
          <li>
            <strong><%= f.name %></strong>
            <div class="file-actions">
              <a href="/download/<%= f.path %>"><i class="fas fa-download"></i> Tải xuống</a>
              <form action="/delete/<%= f.id %>" method="POST" onsubmit="return confirm('Bạn có chắc muốn xóa file này?')">
                <button type="submit"><i class="fas fa-trash-alt"></i> Xóa</button>
              </form>
            </div>
          </li>
        <% }) %>
      <% } %>
    </ul>
  </div>
</body>
</html>
