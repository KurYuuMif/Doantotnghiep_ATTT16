<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Xác thực OTP</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f0f0;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
    }

    .container {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      padding: 40px;
      width: 90%;
      max-width: 500px;
    }

    h2 {
      font-size: 2rem;
      color: #4CAF50;
      margin-bottom: 10px;
    }

    p {
      margin-bottom: 20px;
    }

    input {
      padding: 12px;
      margin: 10px 0;
      width: 100%;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 1rem;
    }

    button {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1rem;
      margin-top: 10px;
    }

    button:hover {
      background-color: #45a049;
    }

    .resend-btn {
      background-color: #007BFF;
    }

    .resend-btn:hover {
      background-color: #0056b3;
    }

    a {
      display: block;
      margin-top: 20px;
      text-decoration: none;
      font-size: 1.1rem;
      color: #007BFF;
    }

    a:hover {
      color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2><i class="fas fa-key"></i> Xác thực OTP</h2>
    <p>Nhập mã OTP đã được gửi đến email của bạn.</p>

    <form method="POST" action="/login">
      <input type="hidden" name="username" value="<%= username %>" />
      <input type="text" name="token" placeholder="Nhập mã OTP" required />
      <button type="submit"><i class="fas fa-paper-plane"></i> Gửi</button>
    </form>

    <form action="/settings/resend-otp" method="POST">
      <button type="submit" class="resend-btn"><i class="fas fa-sync-alt"></i> Gửi lại mã OTP</button>
    </form>

    <a href="/"><i class="fas fa-arrow-left"></i> Quay lại trang chủ</a>
  </div>
</body>
</html>
